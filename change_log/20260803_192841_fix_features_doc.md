# Fix and complete docs/features.md

Implements: `plans/20260803_192656_fix_features_doc.md`

## What changed

Only `docs/features.md` was edited. No code files changed.

1. **Section 1 (App Overview)** — added short mentions of theme switching, per-shortcut usage
   insights (launch count, "Recently launched"/"Most launched" sorts, Activity card), and
   Backup & Restore as a full two-way (export + import) feature. Also clarified that
   single-item quick actions only show when exactly one item is selected.
2. **Section 2 (Build Flavors)** — noted that dev builds show a `-dev` suffix in the version
   name on the About screen.
3. **Section 5.1 (Home Screen)** — fixed the selection counter text (plain number, no word
   "selected"), the empty search-state text ("No matching shortcuts", not "...found"), and the
   single-item quick action button names ("Shortcut details" / "Copy URL", not "View details" /
   "Copy link").
4. **Section 5.6 (About Screen)** — removed the incorrect claim that the screen shows "project
   credits" (it doesn't); described the real Notes card and the combined `version+buildNumber`
   display format instead.

## Why

An audit against the actual source code (`lib/src/screens/home_screen.dart`,
`lib/src/screens/about_screen.dart`, `lib/src/about_constants.dart`,
`android/app/build.gradle.kts`) found the doc text didn't match on-screen strings in a few
places, and Section 1's summary didn't mention a few features that are described in full detail
later in the document.
