# Project Structure — SreerajP YouTube Shortcuts

This living document details the repository layout and layer responsibilities for SreerajP YouTube Shortcuts.

**Read first:** [../CLAUDE.md](../CLAUDE.md) · [architecture.md](architecture.md) for architectural rules and layer boundaries · [`docs/guidelines/guideline.md`](guidelines/guideline.md) §3 for the baseline layout every app follows.

---

## 1. Directory Overview

```
.
├── AGENTS.md                  # Project instructions for AI agents / LLMs
├── CLAUDE.md                  # Project instructions for Claude Code
├── l10n.yaml                  # Localization generator settings (ARB → AppLocalizations)
├── assets/
│   └── config/
│       └── app_config.json    # About screen metadata (source of truth)
├── android/                   # Android native platform project & Gradle configuration
│   ├── key.properties         # Release signing credentials (gitignored)
│   └── keystore.jks           # Release keystore (gitignored)
├── change_log/                # Record of completed changes
├── docs/                      # Architectural, security, and build documentation
│   ├── GUIDELINES_MANIFEST.md # Shared guidelines pointer manifest
│   └── guidelines/            # Shared Flutter guidelines Git submodule
├── lib/
│   ├── app/
│   │   ├── app_shell.dart     # ShortcutApp, FatalApp, and the privacy-lock gate
│   │   └── theme/
│   │       └── app_theme.dart # ThemeData for every AppThemePreference
│   ├── core/
│   │   ├── config/            # AppConfig model & ConfigService loader (fixed path)
│   │   ├── constants/         # Build-time and generated constants (about, version, build date)
│   │   └── errors/            # Sealed domain exceptions
│   ├── l10n/
│   │   ├── app_en.arb         # Base language strings (hand-edited)
│   │   ├── app_localizations*.dart  # Generated by `flutter gen-l10n`
│   │   ├── error_messages.dart# Error code localization mappings
│   │   └── model_labels.dart  # Localized display names for model enums
│   ├── models/                # Immutable domain models & the QR payload parser
│   ├── repositories/          # Data access abstraction & SharedPreferences implementation
│   ├── services/              # Platform & business services (launcher, backup, share, lock)
│   ├── state/                 # ChangeNotifier state holders (ShortcutStore, PrivacyLockStore)
│   ├── screens/               # Full page UI screens
│   │   └── help/              # Help hub and topic-specific guidance screens
│   ├── widgets/               # Reusable UI components, cards, grid, and dialogs
│   └── main.dart              # App bootstrap and global error handling
├── plans/                     # Implementation plans awaiting approval
├── pubspec.yaml               # Flutter package configuration & asset registration
├── test/                      # Unit and widget test suite, mirroring lib/
└── tool/                      # Pre-build metadata generators (version, build date)
```

---

## 2. Layer Responsibilities

| Directory | Holds | Must not |
|---|---|---|
| `lib/app/` | App widget, provider wiring, theme, lock gate | Contain business rules or storage code |
| `lib/core/config/` | `AppConfig` + `ConfigService` (fixed path per guideline §1) | Be moved or renamed |
| `lib/core/constants/` | Compile-time and generated constants | Hold user-visible text |
| `lib/core/errors/` | The sealed `AppException` hierarchy | Throw raw `Exception` or strings |
| `lib/l10n/` | ARB strings, generated lookups, enum label extensions | Be edited by hand except `app_en.arb` and `model_labels.dart` |
| `lib/models/` | Immutable entities, enums, parse results | Hold UI copy, `BuildContext`, or storage details |
| `lib/repositories/` | Reading and writing `SharedPreferences` | Know about widgets |
| `lib/services/` | URL formatting, launching, backup, share intents, biometrics | Know `BuildContext`, navigation, or UI copy |
| `lib/state/` | `ChangeNotifier` stores the UI listens to | Import widget classes |
| `lib/screens/` | Full-page UI and user interaction | Parse URLs or touch `SharedPreferences` |
| `lib/widgets/` | Reusable presentational components and dialogs | Contain business or parsing logic |
| `test/` | Unit and widget tests mirroring `lib/` | — |
| `tool/` | Build-time metadata helper scripts | Depend on Flutter framework code |

---

## 3. Test Layout

`test/` mirrors `lib/` so ownership is obvious:

```
test/
├── models/     # qr_payload_parser_test.dart
├── services/   # backup, privacy lock, youtube url formatter
├── state/      # shortcut store, privacy lock store
└── widget_test.dart
```
